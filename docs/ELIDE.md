# 侧栏路径截短

侧栏按实际等宽字体列宽量预算，不按字符数猜。中日韩字符按两列处理，路径优先保留文件名，
显示被截短时悬停可看完整路径。`crates/core/src/elide.rs` 是纯逻辑，测试里有一个对照实验：
按字符数截中文路径会溢出，按列宽不会。

这是近似的 Unicode East Asian Width，不是完整 UAX #11。组合变音可能被安全侧高估，
结果是文字更早截短而不是溢出。侧栏长路径不会再挤成三行，短文本不改动。
