[package]
name = "yi-edit-core"
description = "Yi Edit 的纯核心：不碰 I/O、不用系统时间、不用随机、不用环境变量"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
# 故意留空，而且必须留空：core 是零依赖的纯核心。
# crates/meta/tests/purity.rs 里有一条断言把「依赖集合 == 空集」钉住。
